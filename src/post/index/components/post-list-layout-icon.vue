<template>
  <i :class="postListLayoutIconClasses" @click="$emit('click', name)"></i>
</template>

<script>
import { defineComponent } from 'vue';
import { mapGetters } from 'vuex';

export default defineComponent({
  name: 'PostListLayoutIcon',

  /**
   * 属性
   */
  props: {
    name: {
      type: String,
    },
  },

  emits: ['click'],

  /**
   * 数据
   */
  data() {
    return {};
  },

  /**
   * 计算属性
   */
  computed: {
    ...mapGetters({
      theme: 'layout/theme',
    }),

    postListLayoutIconClasses() {
      return ['post-list-layout-icon', this.name, this.theme];
    },
  },

  /**
   * 已创建
   */
  created() {
    //
  },

  /**
   * 组件方法
   */
  methods: {},

  /**
   * 使用组件
   */
  components: {},
});
</script>

<style scoped></style>
